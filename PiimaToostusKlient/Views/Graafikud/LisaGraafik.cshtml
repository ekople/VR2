@model PiimaToostusKlient.Models.GraafikModel
@{
    ViewBag.Title = "LisaGraafik";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}

<h2>Lisa voi muuda graafikut</h2><br /><br />
<script>
    $(function () {
        $("#AlgusKP").datepicker({
            showOn: "button",
            changeMonth: true,
            changeYear: true,
            buttonImage: "@Url.Content("~/Content/themes/JqueryDesign/images/calendar.gif")",
            dateFormat:'dd.mm.yy',
            buttonImageOnly: true
        });
        $("#LoppKP").datepicker({
            showOn: "button",
            changeMonth: true,
            changeYear: true,
            buttonImage: "@Url.Content("~/Content/themes/JqueryDesign/images/calendar.gif")",
            dateFormat:'dd.mm.yy',
            buttonImageOnly: true
        });
    });
</script>
<section id="IsikGraafikForm">
    @using (Html.BeginForm(new { ReturnUrl = ViewBag.ReturnUrl }))
    {
        @Html.AntiForgeryToken()
        @Html.ValidationSummary(true)

        <fieldset>
            <legend></legend>
            <ol>
                <li>
                    @Html.LabelFor(m=>m.IsikID)
                   
                    @if (Model.Isikud == null)
                    { @Html.DropDownList("Select", new SelectList(new[] { "-" }, "-")) }
                    else
                    { @Html.DropDownList("Select", Model.Isikud) }

                    @Html.ValidationMessageFor(m => m.IsikID)
               </li>
                <li>
                    @Html.LabelFor(m => m.AlgusKP)
                    @Html.TextBoxFor(m => m.AlgusKP)
                    @Html.ValidationMessageFor(m => m.AlgusKP)
               </li>
               <li>
                    @Html.LabelFor(m => m.LoppKP)
                    @Html.TextBoxFor(m => m.LoppKP)
                    @Html.ValidationMessageFor(m => m.LoppKP)
               </li>
               <li>
                    @Html.LabelFor(m => m.Kommentaar)
                    @Html.TextBoxFor(m => m.Kommentaar)
                    @Html.ValidationMessageFor(m => m.Kommentaar)
               </li>
               <li>
                    @Html.LabelFor(m=>m.AtribuutikaID)
                   
                    @if (Model.Atribuutikad == null)
                    { @Html.DropDownList("Select", new SelectList(new[] { "-" }, "-")) }
                    else
                    { @Html.DropDownList("Select", Model.Atribuutikad) }

                    @Html.ValidationMessageFor(m => m.AtribuutikaID)
               </li>
                <li>
                    @Html.LabelFor(m=>m.OsakondID)
                   
                    @if (Model.Osakonnad == null)
                    { @Html.DropDownList("Select", new SelectList(new[] { "-" }, "-")) }
                    else
                    { @Html.DropDownList("Select", Model.Osakonnad) }

                    @Html.ValidationMessageFor(m => m.OsakondID)
               </li>
            </ol>
            <input type="submit" value="Salvesta" />
        </fieldset>
    }
</section>
<br />
@Html.ActionLink("Tagasi", "Graafikud")


